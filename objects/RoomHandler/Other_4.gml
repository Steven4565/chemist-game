// Enable Viewports
view_enabled = true;

// Enable a specific viewport
var view_id = 0; // Using Viewport 0
view_visible[view_id] = true;

view_camera[0] = global.cam;

//// Create the camera
//var camera_width = 683;
//var camera_height = 384;
//var viewport_width = 1366;
//var viewport_height = 768;
//var horz_border = 500;
//var vert_border = 500;

//// Create a camera
//var camera = camera_create_view(0, 0, camera_width, camera_height, 0, oPlayer, -1, -1, horz_border, vert_border);

//// Set the viewport
//view_set_visible(view_id, true);
//view_set_camera(view_id, camera);
//view_set_wport(view_id, viewport_width);
//view_set_hport(view_id, viewport_height);

//// Set the viewport position and size
//view_set_xport(view_id, 0);
//view_set_yport(view_id, 0);
//view_set_wport(view_id, viewport_width);
//view_set_hport(view_id, viewport_height);